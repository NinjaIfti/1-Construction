// Basic navbar functionality (keeping it minimal)
document.addEventListener('DOMContentLoaded', function() {
    // Mobile menu toggle handled by Bootstrap
    // No additional JavaScript needed for the simple navbar
}); 